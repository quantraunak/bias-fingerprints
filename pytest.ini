[pytest]
# Tests import `src.*`, which lives under project/. Without this the suite runs
# from project/ and fails from the repository root -- which is exactly how CI
# invokes it, so CI was collecting zero tests and reporting an error nobody read.
pythonpath = project
testpaths = project/tests
